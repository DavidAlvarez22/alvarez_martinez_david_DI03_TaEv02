import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alvarez_martinez_david_DI03_TaEv02',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
